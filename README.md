# 🇲🇬 CSS Flag of Madagascar

A simple **HTML + CSS project** that recreates the **national flag of Madagascar** using styled div elements. The flag consists of a vertical white stripe on the left and two horizontal stripes (red and green) on the right.

## ✨ Features
- **Accurate Colors**  
  - White → `#FFFFFF`  
  - Red → `#FC3E32`  
  - Green → `#007E39`  

- **Flag Layout**  
  - Left vertical stripe: White  
  - Right top stripe: Red  
  - Right bottom stripe: Green  

- **Customizable Variables**  
  - Colors and flag dimensions defined in `:root` for easy adjustments.  
  - `--flag-height` and `--flag-width` allow resizing while maintaining proportions.  

- **Responsive Centering**  
  - Flexbox centers the flag both vertically and horizontally on the page.  

- **Minimal Structure**  
  - Simple HTML with three styled `<div>` elements for the stripes.  
  - Lightweight CSS for layout and coloring.  

## 🛠️ Tech Stack
- **HTML5**  
- **CSS3 (Flexbox + Variables)**  

## 🚀 Use Cases
- Practicing CSS layout and positioning  
- Demonstrating how to build flags with pure CSS  
- Educational projects for learning Flexbox and CSS variables  
- Creative UI elements for themed websites  

## 📂 Project Structure
```
index.html   # Main HTML structure
style.css    # Flag styling and layout
```
